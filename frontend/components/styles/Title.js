import styled from 'styled-components';

const Title = styled.h3`
  margin: 0;
  text-align: left;
  font-weight: 300;
  a {
    color: var(--green);
    display: inline;
    line-height: 1.3;
    font-size: 2.5rem;
    text-align: left;
    padding: 0 1rem;
  }
`;

export default Title;
